const sayHello = (name) => "Hello, " + name + "!";

console.log(sayHello("Sukesh"));

const tripleNumber = (num) => 3 * num;

console.log(tripleNumber(10));

const check = (word) => word[0] === "S";

let name = "Sukesh";

console.log(check(name));